<div class="lista-produtos">
  <ul>
   <% vendedores.each do |vendedor| %>
      <div class="item-lista">
        <img src="<%= asset_path('folder_icon.png') %>" alt="iconezinho windows">
        <li><%= link_to "#{vendedor.user.nome}", vendedor_path(vendedor) %></li>
      </div>
    <% end %>
  </ul>
</div>